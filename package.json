{
  "name": "nicksneed",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cls && npx stylelint src/**/*.scss && npm run compile:sass && eslint src && npm run compile:js",
    "compile:sass": "sass --no-source-map --style=compressed src/:dist/",
    "compile:js": "webpack",
    "deploy": "node confirmDeploy.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "eslint": "^9.17.0",
    "gh-pages": "^6.2.0",
    "sass": "^1.83.0",
    "stylelint": "^16.12.0",
    "stylelint-config-standard": "^36.0.1",
    "stylelint-scss": "^6.10.0",
    "webpack": "^5.97.1",
    "webpack-cli": "^6.0.1"
  }
}
